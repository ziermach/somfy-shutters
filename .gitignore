# editors
.idea/
.vscode/
*.swp

# macOS
.DS_Store

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# node
node_modules/
dist/
build/

# runtime — never commit these
*.db
*.db-shm
*.db-wal
*.sqlite3
operateShutters.conf
.env

# runtime config holds real RTS addresses — never commit
config/shutters.toml
# written by the app: measured travel times, not source
config/calibration.toml

# written by the app, holds measured travel times
config/calibration.toml

# git worktrees live here; they are checkouts, not content
.claude/worktrees/
